@Library('todo') _

roboshop (
        COMPONENT             : 'frontend',
        PROJECT_NAME          : "todo",
        SLAVE_LABEL           : "MASTER",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NGINX"
)
