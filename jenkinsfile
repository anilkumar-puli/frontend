@Library('todoapp') _

roboshop (
        COMPONENT             : 'frontend',
        PROJECT_NAME          : "todoapp",
        SLAVE_LABEL           : "MASTER",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NGINX"
)
